<h1>Listar Pagamento</h1>


{{#each pagamentos}}
	ID:{{dataValues.id}}<br>
	Nome:{{dataValues.nome}}<br>
	Valor:{{dataValues.valor}}<br>
	Cadastrado:{{#formatDate dataValues.createdAt}}{{/formatDate}}<br><br>
	<a href="/del-pagamento/{{dataValues.id}}"><button class="btn btn-outline-danger">Apagar</button></a>
	<hr>
{{/each}}

